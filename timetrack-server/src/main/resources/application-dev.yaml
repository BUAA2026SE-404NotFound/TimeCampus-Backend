spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/timetrack?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: 1595285306aA
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      database: 0
      timeout: 3s

logging:
  level:
    root: info
    com.notfound: debug

wechat:
  appid: ${WECHAT_APPID:}
  secret: ${WECHAT_SECRET:}

storage:
  local-root-dir: ./uploads
  max-file-size-mb: 20
